# Core (same as original)
anthropic>=0.39.0
httpx>=0.27.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
websockets>=13.0
playwright>=1.40.0
pyyaml>=6.0

# SSL cert generation
cryptography>=42.0.0

# Windows system integration
pywin32>=306; sys_platform == "win32"
psutil>=5.9.0

# Screenshot (install at least one)
mss>=9.0.1
Pillow>=10.0.0

# TTS (edge-tts — free Microsoft neural voices, no API key)
edge-tts>=6.1.9
